# Prevent directory listing
Options -Indexes

# Protect sensitive files
<FilesMatch "^(bootstrap|\.env)">
    Require all denied
</FilesMatch>

# Enable rewrite (optional)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /clinic-management/
</IfModule>
